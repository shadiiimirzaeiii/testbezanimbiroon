# #!/bin/sh
# . "$(dirname "$0")/_/husky.sh"

# # Run any pre-commit tasks here (e.g., linting, testing)
# # Example: npx eslint .

# # Display a custom message
# echo "✔ Pre-commit checks passed! Your code is ready to commit."
#!/bin/sh
. "$(dirname "$0")/_/husky.sh"
echo "Running pre-commit hook"
echo 'Pre-commit hook running'
